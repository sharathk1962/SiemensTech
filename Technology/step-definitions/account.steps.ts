import { Then, When } from "@cucumber/cucumber";

When(`I click on the account symbol in the left corner`, async () => {
});

When(`I select {string} from the dropdown`, async () => {
});

When(`I select {string} option`, async () => {
});

Then(`The background should change to dark`, async () => {
    });

Then(`The background should change to white`, async () => {
   });

   Then(`The theme should change to red on the left top`, async () => {

});

Then(`The theme should change to neutral`, async () => {
  //  const backgroundColor = await themeElement.getCSSProperty('background-color');
    // expect(backgroundColor.value).toBe('rgb(128, 128, 128)'); // Neutral gray color in RGB
});


